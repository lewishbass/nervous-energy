// GraphVisualizer
// takes a graph object
// nodes should have data and approximate position
// edges should have two nodes, weight and direction (or no direction)

// uses re-charts to render the graph and arrange nodes

